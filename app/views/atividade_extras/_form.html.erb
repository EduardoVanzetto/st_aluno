<%= form_with(model: atividade_extra, local: true) do |form| %>
  <% if atividade_extra.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(atividade_extra.errors.count, "error") %> prohibited this atividade_extra from being saved:</h2>

      <ul>
      <% atividade_extra.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :curso_id %>
    <%= form.text_field :curso_id %>
  </div>

  <div class="field">
    <%= form.label :disciplina_id %>
    <%= form.text_field :disciplina_id %>
  </div>

  <div class="field">
    <%= form.label :nota %>
    <%= form.text_field :nota %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
