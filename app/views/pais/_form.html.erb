<%= form_with(model: pal, local: true) do |form| %>
  <% if pal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pal.errors.count, "error") %> prohibited this pal from being saved:</h2>

      <ul>
      <% pal.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome %>
  </div>

  <div class="field">
    <%= form.label :cpf %>
    <%= form.text_field :cpf %>
  </div>

  <div class="field">
    <%= form.label :rg %>
    <%= form.text_field :rg %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
